## Why

`.gitignore` 缺少 `.claude/` 和 `.VSCodeCounter/` 等目录，每次 `git status` 都显示为 untracked 文件，干扰工作。

## What Changes

- `.gitignore` 追加 `.claude/`、`.VSCodeCounter/`

## Capabilities

### New Capabilities

（无）

### Modified Capabilities

（无）

## Impact

- `.gitignore` 加 2 行
