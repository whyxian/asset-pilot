---
description: 执行代码任务前必须先读根上下文文档
paths:
  - "*"
---

# 先读上下文

执行任何代码分析/修改前，必须先读根目录的 CLAUDE.md。

**理由**：CLAUDE.md 是项目的总索引，包含项目结构、编码约定（含红线：禁止删数据库文件、超时铁律、先讨论后实现）与关键机制，遗漏会直接违反项目约束。

**例外**：与代码无关的通用问答可跳过。
