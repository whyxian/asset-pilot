# AssetPilot 代码审查报告

> 审查时间：2026-06-21
> 审查范围：unused imports / 死代码 / 未使用 UI 组件
> 状态：已修复

---

## 🟢 轻微问题（未使用 import / 死代码）

### 1. `snapshot_service.py:21` — OverviewService 已删残留 import

`_calc_annualized` 已删，`OverviewService` import 成了死代码。

### 2. `snapshot_repository.py:8` — `desc` 未使用

`from sqlalchemy import desc, select` 中 `desc` 从未使用。

### 3. `asset_quote_service.py:14` — `QuoteCache` 类未使用

`from app.utils.quote_cache import QuoteCache, quote_cache` 中只用了 `quote_cache` 实例，`QuoteCache` 类名从未引用。

### 4. `components/ui/sheet.tsx` — shadcn 模板未使用

`Sheet` 组件已注册但全项目无任何页面引用，是 shadcn/ui 模板残留。

### 5. `asset_quote_repository.py:79` — `get_recent_quotes()` 死方法

被 `quote_cache` 接管后，生产代码无调用方，仅测试调用。测试也只验证 DB 查询逻辑，与当前架构无关。

### 6. `database.py:30-36` — 8 个 ORM import 带 `# noqa: F401`

`init_db()` 内的 ORM import 是**有意的副作用导入**（`Base.metadata.create_all` 需要 ORM 模型先注册）。保留，不删除。

---

## 已修复

| # | 文件 | 操作 |
|---|------|------|
| 1 | `backend/app/services/snapshot_service.py` | 删除 unused `OverviewService` import |
| 2 | `backend/app/repositories/snapshot_repository.py` | 删除 unused `desc` import |
| 3 | `backend/app/services/asset_quote_service.py` | 删除 unused `QuoteCache` class import，只保留 `quote_cache` 实例 |
| 4 | `frontend/src/components/ui/sheet.tsx` | 删除未使用的 Sheet 组件文件 |
| 5 | `backend/app/repositories/asset_quote_repository.py` | 删除 `get_recent_quotes()` 死方法（含注释引用清理） |
| 5 | `backend/test/test_asset_quote_repository.py` | 删除 `get_recent_quotes` 对应的两个测试 |

## 保留不删

| 文件 | 原因 |
|------|------|
| `core/database.py` ORM imports | `init_db()` 内副作用导入，`Base.metadata.create_all` 需要。`# noqa: F401` 标注合理 |
| `exchange_rate.py:convert/to_usd/from_usd/to_cny/fetch_rates_snapshot` | `snapshot_service.py:270-271` 仍在调用 `convert`；`fetch_rates_snapshot` 仍在 `snapshot_service:47` 调用；`to_usd/from_usd/to_cny` 是 `convert` 的 wrapper，保持 API 兼容 |
